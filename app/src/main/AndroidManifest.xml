<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mio.a20180122test">

    <application
        android:allowBackup="true"
        android:icon="@drawable/post_it_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/post_it_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        >
        <activity android:name=".MainActivity"
            android:theme="@style/testtheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity_list" />
        <activity android:name=".OrderRecord_page" />
        <activity android:name=".Activity_list_detail"></activity>
    </application>

</manifest>